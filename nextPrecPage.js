function createNextPreButton(){
  const questionResponseContainer = Array.from(document.querySelectorAll('.question-response'));
  questionResponseContainer.map((qr,index)=>{
    
  })
}

const nextPage(){


}